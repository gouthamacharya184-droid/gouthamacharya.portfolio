{
  "name": "goutham-portfolio-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix backend install",
    "build": "npm --prefix backend install && npm --prefix frontend run build",
    "start": "node backend/api/server.js",
    "install-deps": "npm install --prefix backend && npm install --prefix frontend",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev"
  }
}
